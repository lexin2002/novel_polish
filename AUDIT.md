# 🔍 代码审计报告 (AUDIT.md)

本报告旨在识别项目中的冗余代码、死代码、孤立文件以及前后端功能不对称点。
**审计原则**：仅标记，不删除。

---

## 1. 📉 后端 $\rightarrow$ 前端 功能不对称 (Backend-to-Frontend Gap)
以下 API 端点在后端已实现，但在前端 `src/` 源码中未发现明确的调用链路。

| 端点 | 描述 | 状态 | 建议 |
|---|---|---|---|
| `GET /api/health` | 健康检查 | 🔴 未调用 | 可用于前端启动时的心跳检测 |
| `GET /api/config/path` | 获取配置路径 | 🔴 未调用 | 仅用于调试，建议保留或在设置页显示 |
| `GET /api/history/count` | 获取历史记录总数 | 🔴 未调用 | 可在历史列表页显示总数 |
| `GET /api/history/{snapshot_id}` | 获取快照详情 | ⚠️ 部分调用 | `historyStore.ts` 有调用，但 UI 界面可能未完全展现其详情 |

---

## 2. 🎨 前端 $\rightarrow$ 后端 功能不对称 (Frontend-to-Backend Gap)
以下 UI 交互或逻辑在前端已存在，但在后端 API 中未发现对应的实现。

| UI 模块 | 功能点 | 状态 | 建议 |
|---|---|---|---|
| 全局 | 无 | ✅ 对称 | 当前所有触发 API 的 UI 均有对应后端端点 |

---

## 3. 🗑️ 死代码与废弃资产 (Dead Code & Assets)

### 3.1 孤立文件 (Orphaned Files)
- **结果**: 未发现完全孤立的文件。所有 `.py`, `.ts`, `.tsx` 文件均在项目调用链中。

### 3.2 废弃/无用代码段
- **后端**: 
  - `backend/app/api/rest.py` 中关于 `initialize_polishing_service` 的紧急初始化逻辑在大部分场景下被 `test-connection` 覆盖，但作为兜底机制建议保留。
- **前端**: 
  - 某些 `index.ts` 导出文件仅包含单行导出，属于结构性冗余而非功能性死代码。

---

## 4. 📝 综合审计结论

**项目整体健康度：极高** $\checkmark$
- 前后端映射关系基本对齐。
- 绝大多数 API 均有对应的 UI 触发点。
- 未发现严重的死代码块或无用文件。

**待优化建议**：
- 建议在前端 `Archive.tsx` (历史记录页) 增加 `history/count` 的显示。
- 建议在系统启动时调用 `/api/health` 确保后端就绪。

---
*审计日期：2026-04-29*
*审计状态：已完成全量扫描*
